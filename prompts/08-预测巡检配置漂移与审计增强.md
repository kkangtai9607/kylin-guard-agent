# Phase 8：增强功能

```text
本次只执行 Phase 8。完成后停止。

实现：

1. 周期系统快照；
2. 磁盘增长趋势；
3. 内存持续增长；
4. 服务失败次数趋势；
5. EWMA；
6. Z-score；
7. 轻量异常预警；
8. 配置基线；
9. 配置漂移检测；
10. 审计事件哈希链；
11. 审计完整性校验；
12. JSON 导出；
13. 风险和趋势页面接入；
14. 相关测试。

约束：

- 不引入大型训练模型；
- 不依赖 GPU；
- 不让预测结果自动触发写操作；
- 趋势结论必须显示数据窗口和依据；
- 配置文件内容必须脱敏；
- 哈希链使用稳定 canonical JSON；
- 管理员操作同样写入审计；
- 完整性校验失败必须明确报警。

验收：

- 周期快照可配置；
- 趋势结果可重复；
- 配置漂移能展示 diff 摘要；
- 审计链可验证；
- 篡改测试能被发现；
- 前后端集成通过。

更新状态和文档后停止。
```
