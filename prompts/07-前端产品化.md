# Phase 7：前端产品化

```text
本次只执行 Phase 7：Vue 3 前端和真实 API 接入。完成后停止。

实现页面：

1. 登录；
2. 运维驾驶舱；
3. 智能运维对话；
4. 任务时间线；
5. 安全审批中心；
6. MCP 插件中心；
7. 安全巡检；
8. 故障事件与 RCA；
9. 配置漂移；
10. 知识库；
11. 审计日志；
12. 系统设置。

要求：

- Vue 3 + TypeScript + Vite；
- Pinia 和 Router；
- Element Plus；
- ECharts；
- 使用真实 API，不以静态假数据代替核心流程；
- DEMO 数据必须有明显标识；
- SSE/WebSocket 状态更新；
- 网络断开和重连；
- 错误提示；
- 权限控制；
- 审批动作二次确认；
- 风险等级清晰；
- 展示结构化计划、证据和理由摘要；
- 不展示隐藏思维；
- 适配 1920×1080 比赛演示；
- 生产构建后不依赖 node_modules 运行。

重点打磨一条演示闭环：

用户提出“帮我清理系统垃圾”
→ 环境感知
→ 文件候选
→ 关键日志拦截
→ 根因分析
→ dry-run
→ L3 审批
→ 安全执行
→ 验证
→ 审计时间线

验收：

- 类型检查通过；
- 构建通过；
- 核心 API 接入；
- 任务状态实时更新；
- 审批可操作；
- 风险和证据清晰；
- 主要页面无明显空白或假按钮；
- 后端失败时有合理提示。

更新状态后停止。
```
