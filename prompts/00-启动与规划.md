# Phase 0A：只规划，不改文件

在 Codex 中先进入 Plan 模式，然后提交：

```text
阅读 @AGENTS.md、@PROJECT_SPEC.md、@PLANS.md 和 @CURRENT_STATUS.md。

当前任务仅为 Phase 0 的规划。不要修改任何文件，不要生成业务代码，不要进入 Phase 1。

请检查当前仓库状态，并输出一份可以直接执行的 Phase 0 实施计划，必须包括：

1. 当前仓库已有文件和可复用内容；
2. 推荐目录结构；
3. Phase 0 需要创建或修改的文件；
4. 总体架构文档；
5. 业务闭环；
6. 威胁模型和信任边界；
7. 风险等级；
8. Agent 状态机；
9. MCP Tool 元数据规范；
10. 数据库 ER 设计；
11. API 草案；
12. 配置文件设计；
13. DEMO、READ_ONLY、CONTROLLED_EXECUTION 三种模式；
14. 比赛评分点映射；
15. Phase 0 验收标准；
16. 计划执行的检查命令；
17. 当前环境无法验证的麒麟 V11 与 LoongArch 内容；
18. 主要技术风险及应对方案。

重点审查：

- 是否可能将 LLM 输出直接执行；
- 是否存在任意 Shell 设计；
- 安全护栏是否独立于模型；
- 写操作是否强制审批；
- 是否存在备份、验证和回滚；
- 是否会误存模型隐藏思维过程；
- 日志和知识库中的间接提示词注入如何隔离；
- 是否引入不适合 LoongArch 的硬依赖；
- 是否能稳定完成 7 分钟比赛演示。

只输出计划。完成后停止，等待我的执行指令。
```

# Phase 0B：执行架构阶段

```text
按照已经确认的 Phase 0 计划执行。

范围限制：

- 只完成 Phase 0；
- 不实现 Phase 1 业务代码；
- 不提前实现 LLM 调用、MCP Tool 或前端页面；
- 不引入未经验证的重型依赖；
- 不覆盖已有有效文件；
- 完成后必须停止。

必须创建或完善：

1. README.md；
2. docs/01-软件功能需求分析.md；
3. docs/02-软件功能设计.md 的架构部分；
4. docs/08-安全设计说明.md；
5. docs/09-比赛评分点映射.md；
6. docs/architecture/总体架构.md；
7. docs/architecture/业务闭环.md；
8. docs/architecture/Agent状态机.md；
9. docs/architecture/威胁模型.md；
10. docs/architecture/数据库ER设计.md；
11. docs/07-接口文档.md 的 API 草案；
12. config/ 下的配置样例；
13. .env.example；
14. PLANS.md；
15. CURRENT_STATUS.md；
16. docs/architecture/adr/0001-初始技术选型.md。

文档必须明确：

- 模型没有最终执行权限；
- 禁止任意 Shell；
- 三种运行模式；
- 双层安全护栏；
- 最小权限；
- 人工审批；
- 备份、验证、回滚；
- 结构化决策链而不是隐藏思维过程；
- 麒麟 V11 与 LoongArch 兼容策略；
- DEMO 场景；
- 九项比赛交付物。

完成后：

1. 检查文档之间是否冲突；
2. 检查目录和文件引用；
3. 更新 CURRENT_STATUS.md；
4. 报告实际创建和修改的文件；
5. 报告实际运行的检查命令；
6. 列出遗留问题；
7. 停止，不得进入 Phase 1。
```
