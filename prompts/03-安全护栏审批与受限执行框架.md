# Phase 3：安全护栏、审批与受限执行框架

```text
本次只执行 Phase 3。完成后停止，不实现真实写操作 Tool。

目标：

1. 输入安全检测；
2. 风险等级 L0-L4；
3. 确定性 Policy Engine；
4. Tool 参数安全；
5. Path Guard；
6. protected paths；
7. 服务白名单；
8. PID 关键进程保护；
9. 意图一致性校验接口；
10. RBAC 加强；
11. Approval 数据模型和接口；
12. HMAC 一次性审批令牌；
13. RestrictedExecutor 框架；
14. dry-run 数据结构；
15. backup、verification、rollback 接口；
16. 安全审计事件；
17. 安全测试集。

必须阻断：

- 凭据读取；
- 绕过安全规则；
- 任意 Shell；
- 命令替换、管道和重定向；
- 路径穿越；
- 符号链接逃逸；
- protected paths；
- 参数审批后篡改；
- 只读目标升级为写动作；
- 用户目标与计划不一致；
- 重复或过期审批令牌。

RestrictedExecutor 当前只实现框架和无害测试适配器，不执行真实系统变更。

审批令牌必须绑定：

- user_id
- task_id
- tool_name
- canonical arguments hash
- risk level
- expiration
- nonce

测试至少覆盖：

- 高危输入；
- Unicode 和 Base64 混淆；
- 路径规范化；
- protected paths；
- 符号链接；
- Token 过期；
- Token 重放；
- 参数篡改；
- 角色越权；
- 间接提示词注入；
- 规则引擎否决模型建议。

验收：

- L4 请求不调用 MCP；
- L3 动作没有审批不能进入执行态；
- 只读任务不能被模型提升为写操作；
- shell=True、eval、exec 扫描为 0；
- 安全测试通过；
- 审计记录完整。

完成后更新 CURRENT_STATUS.md、PLANS.md 和安全设计文档，然后停止。
```
