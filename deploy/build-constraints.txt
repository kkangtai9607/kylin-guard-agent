# Build-backend constraints for Kylin/LoongArch source builds.
#
# Newer maturin releases use Rust edition2024 and fail with the Cargo 1.82
# toolchain currently available on the official LoongArch VM.  Pydantic-core
# and rpds-py may need maturin only inside pip's isolated build environment, so
# keep the build backend pinned separately from runtime requirements.
maturin==1.8.7
